# this program stores the names and ages of friends in a list and manipulates the data

friends_names = ['kweku', 'truman', 'john']
print(friends_names[0])  # display the first item in the list
print(friends_names[2])  # displays the last item in the list
print(len(friends_names))

# list containing the ages of friends names

friends_ages = ['32', '44', '42']
friends_names.extend(friends_ages)

